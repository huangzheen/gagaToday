# 人工审核任务

**批次**：munich_20260621_initial_audit
**日期**：2026-06-21
**紧急度**：中（不阻塞 MVP，但影响美术质量）

## 必须决策（高优先级）

### 决策 1：Allianz.png 版权风险

**问题**：Allianz.png 涉及真实保险品牌（Allianz SE），版权风险中等。游戏 UI 中使用真实品牌 logo 可能涉及商标侵权。

**选项**：
- A. 替换为通用保险图标（推荐）：画一个通用风格的"盾牌 + 勾"图标，无品牌名
- B. 保留但加 disclaimer：仅在游戏内"参考"，标明"非 Allianz 官方合作"
- C. 删除该图：从 `assets/references/user_2026_06_21/` 删除，归档到 `docs/archive/`

**建议**：A（最安全）

### 决策 2：Anna / Peter 角色复用

**问题**：Anna 和 Peter 角色立绘是 AI 矩阵为"柏林咖啡馆服务员"和"柏林场景男性角色"生成的，慕尼黑 MVP 需要重新定位：

- Anna 原为咖啡馆服务员 → 慕尼黑面包店店长？图书馆管理员？食堂店员？
- Peter 原为柏林场景 → 慕尼黑学校老师？德国同学？邻居？

**选项**：
- A. 复用 Anna 作为慕尼黑面包店店长（npc_anna_bakery_placeholder），Peter 作为德国同学（npc_peter_placeholder）—— 工作量小
- B. 全部重画，慕尼黑专用 —— 工作量大但视觉一致性最好
- C. Anna 用作慕尼黑（npc_anna_bakery_placeholder），Peter 重画 —— 折中

**建议**：A（先 MVP 复用，Phase 2 精修）

### 决策 3：10 张 image*.png 通用参考图归类

**问题**：`image.png` / `image0.png` / `image2-8.png` 共 10 张用户提供的图，文件名无明确语义，未归类到具体用途。

**选项**：
- A. 用户人工查看后给每张图指派标签（character / scene / style / 弃用）
- B. 全部归档到 `docs/archive/unclassified_2026_06_21/`，仅保留已分类的 5 张（慕尼黑大教堂、U-Bahn、在家、餐厅、Allianz）
- C. AI 视觉识别后归类（精度可能不高）

**建议**：A（最准确）

## 建议决策（中优先级）

### 决策 4：缺失素材生成时机

**问题**：24 个 critical / high 优先级缺失素材（10 场景 + 6 角色 + 8 UI）尚未生成。

**选项**：
- A. 在 P9-P10 阶段（Game Core 接入 + Frontend UI）同步生成
- B. 单独开一个批次用 matrix MCP 批量生成（参考 ART_ASSETS.md §AI 批量生成工作流）
- C. 不生成，先用 placeholder 撑过 MVP，Phase 2 精修

**建议**：B（先用 _test 试风格，再批量）

### 决策 5：AI 占位稿实际尺寸处理

**问题**：AI 矩阵生成的图实际尺寸（896x1200 角色 / 1376x768 场景 / 1024x1024 UI）比 ART_ASSETS.md 规范（192x256 / 1280x720 / 9-slice UI）大。

**选项**：
- A. 前端 import 时显式缩放至规范尺寸（保持原图，运行时性能略低）
- B. 生成时直接用 192x256 重新生成（更省空间，但 AI 输出精度可能下降）
- C. 保留高分辨率作为 master，复制低分辨率到 `assets/`（磁盘双倍）

**建议**：A（保留原图，前端缩放）

## 后续任务（低优先级 / 建议）

### 任务 6：去重

`image2.png` 和 `image3.png` 文件大小完全相同（1537852 bytes），可能是同源图。可计算 md5 后确认。

### 任务 7：补充 schema

提交 ArtAsset 模型到 Content Schema（详见 迁移建议.md），由架构 agent 合并。

### 任务 8：manifest 字段扩展

未来可加：
- `usage_count`（被引用次数）
- `last_used_in_build`（最后在 build 中使用）
- `replacement_id`（被替代的旧 ID）

## 阻塞项

**当前无阻塞**。所有 draft 输出都 `review_status: "draft"`，未触碰 published 内容。可以继续推进 P3-P6（数据 agent）而无需等待这些决策。

## 时间窗口

**建议 1 周内回复决策 1-3**（影响后续 P9-P10 的素材生成）。
**决策 4-5 可在 P9 启动前回复**。

## 联系人

- 主协调 agent：Mavis (Mavis / main)
- 美术资产 agent：本批次的产出方
- 架构 agent：负责 schema 迁移
- NPC agent：负责 Anna / Peter 角色复用决策

## 附件

- `运行总结.md` — 本批次的完整总结
- `合规报告.md` — 详细合规检查
- `迁移建议.md` — ArtAsset schema 建议
- `frontend/src/content/drafts/art/munich_20260621_initial_audit/*.draft.json` — 所有 draft 数据
- `assets/references/user_2026_06_21/*.png` — 15 张用户散图副本
