# gagaToday 代码审计与整改方案

> 项目：`/Volumes/NewDisk/GermanLearning`  
> 审计日期：2026-06-30  
> 审计范围：Phase 1–3 MapLibre 玩家客户端、FastAPI Runtime API、内容 schema、玩家状态与现有测试  
> 文档用途：交给后续开发 Agent 逐项整改和验收  
> 注意：本次审计没有修改项目代码

---

## 1. 审计目的

本次审计不是评价代码“写了多少”，而是判断上一轮迁移是否已经形成一条真实、可靠、可继续扩展的产品链路：

```text
SQLite 已发布内容
  → FastAPI Runtime Export
  → CityBundle API
  → 前端 Zod 校验
  → MapLibre 地图和 POI
  → 玩家交互
  → 游戏规则与存档
```

审计目标：

1. 找出自动化测试未发现的运行时错误；
2. 检查前后端 schema 是否真正兼容；
3. 检查地图实现是否能支持未来规模；
4. 检查所谓“玩家状态机”是否已经形成独立 Game Core；
5. 检查移动端、缓存、存档、资产和工程卫生；
6. 给出可以直接执行的修正项目、示例代码、测试和验收标准。

---

## 2. 审计结论

### 2.1 当前真实状态

MapLibre + PMTiles 技术路线已经验证成功，德国真实地图能够渲染，桌面端 UI 视觉完成度较好。

但当前尚不能认定 Phase 1–3 完整交付。更准确的状态是：

> **地图技术原型完成；Runtime API 已建立但前后端契约不通；玩家 UI 已建立但 RPG 闭环仍是占位。**

### 2.2 已通过的检查

| 检查 | 结果 |
|---|---|
| `npm run typecheck` | 通过 |
| 前端 Vitest | 17/17 通过 |
| 后端 Pytest | 59/59 通过 |
| HTTP smoke | 28/28 通过 |
| Vite production build | 通过 |
| PMTiles HTTP Range | 206 正常 |
| MapLibre 浏览器渲染 | 正常 |
| OSM/Protomaps 署名 | 可见 |
| 桌面 POI/HUD/场景图 | 可见 |

### 2.3 自动化测试未发现的实际故障

浏览器实测发现：

- 后端返回 HTTP 200；
- 前端 Zod 拒绝后端 CityBundle；
- App 自动退回静态 fixture；
- 页面显示 `[fallback]`，并非真实 Runtime API 数据；
- 灰色“未发现”POI 仍可点击并立即变成已发现；
- 390×844 移动端视口下顶部、地图控件和 HUD 被裁切；
- “开始对话”仍然只是 `alert()`。

因此当前 smoke 的全绿不能代表用户主链路可用。

---

## 3. 优先级定义

| 优先级 | 含义 |
|---|---|
| P0 | 阻断真实运行链路，下一阶段开发前必须修复 |
| P1 | 不立即阻断原型，但会造成逻辑错误、扩展失败或严重体验问题 |
| P2 | 工程质量、性能和长期维护问题，可在黄金闭环之后处理 |

整改顺序必须是 P0 → P1 → P2。不要绕过 P0 去扩展更多 POI 或城市。

---

## 4. P0-01：FastAPI 输出无法通过前端 Zod

### 4.1 现象

浏览器控制台实际错误：

```text
Zod: pois.0.name.en: Expected string, received null
pois.0.description: Expected object, received null
pois.0.iconUrl: Invalid input
pois.0.audioUrls.de: Invalid input
...
```

客户端随后执行 fallback：

```text
[App] backend fetch failed, using static fallback
```

### 4.2 根因

TypeScript 使用 `.optional()`，允许字段缺失，但不允许 `null`：

```ts
en: z.string().optional()
iconUrl: SceneUrlSchema.optional()
```

Python Pydantic 模型使用 `Optional`，序列化时默认输出 `null`：

```py
en: Optional[str] = None
iconUrl: Optional[str] = None
```

当前 `to_runtime_json()`：

```py
return self.model_dump(mode="json")
```

因此两个所谓“严格对齐”的 schema 实际拥有不同的 JSON 语义。

### 4.3 推荐修正

运行时 API 应省略没有值的可选字段。修改：

`backend/poi-generator/schemas/game_content.py`

```py
def to_runtime_json(self) -> Dict:
    return self.model_dump(
        mode="json",
        exclude_none=True,
    )
```

不推荐把前端所有字段改成 `.nullish()`，因为这会让客户端长期承担两种空值表达。API 应只有一种规范。

如果业务确实需要区分“未知”和“明确为空”，才应对特定字段使用 `null`，不能全局混用。

### 4.4 必须增加的测试

后端测试不能只检查字段名，应确认 POI 可选字段不会以不被 Zod 接受的 `null`
输出。不要全局禁止 `null`，因为对话的 `nextNodeId` 在终止选择中可以合法为 `null`：

```py
def test_runtime_bundle_omits_optional_nulls(client):
    response = client.get("/api/game/v1/cities/munich/bundle")
    assert response.status_code == 200
    for poi in response.json()["pois"]:
        if "en" in poi["name"]:
            assert isinstance(poi["name"]["en"], str)
        if "description" in poi:
            assert isinstance(poi["description"], dict)
        if "iconUrl" in poi:
            assert isinstance(poi["iconUrl"], str)
        for url in poi.get("audioUrls", {}).values():
            assert isinstance(url, str)
```

前端必须测试“实际后端形状”，不能只测试手写 fixture。现有
`export_munich_fixture.py` 是 Phase 0 脚本，它自行拼装 JSON，不能证明 Runtime Exporter
与 Zod 兼容。应新增 `export_runtime_fixture.py`，内部直接调用
`runtime_export_service.export_city()`，再输出它的 `to_runtime_json()`：

```bash
./.venv/bin/python \
  backend/poi-generator/scripts/export_runtime_fixture.py \
  munich \
  --output frontend/game-client/src/test/fixtures/runtime-api-munich.json
```

然后由 Zod 解析：

```ts
it('parses the bundle generated by the Python exporter', () => {
  const result = safeParseBundle(runtimeApiFixture)
  expect(result.ok).toBe(true)
})
```

### 4.5 验收标准

- 浏览器顶部显示 `[network]`，不能是 `[fallback]`；
- 控制台没有 Zod warning；
- FastAPI 实际响应能通过 `CityBundleSchema`；
- HTTP smoke 增加 schema 解析，而不只是检查 200 和字段名；
- ETag 缓存后的第二次加载显示 `[cache]` 或 `[network]`，仍不能 fallback。

---

## 5. P0-02：项目尚无真实 RPG 闭环

### 5.1 现状

以下 exporter 仍直接返回空数组：

```py
_load_npcs() -> []
_load_dialogues() -> []
_load_quests() -> []
_load_knowledge_cards() -> []
```

前端“开始对话”按钮调用：

```ts
alert(`Phase 4 实现对话系统...`)
```

当前 Player Store 有资源字段，但没有：

- Dialogue Engine；
- Quest Engine；
- Condition Evaluator；
- Reward Engine；
- 任务幂等性；
- 学习结果；
- 知识卡发放。

`src/core/` 目录目前为空。核心规则仍集中在 Pinia store，而不是纯 TypeScript Game Core。

### 5.2 修正目标

只实现一个黄金 POI，不批量铺开：

```text
接到任务
  → 地图出现目标
  → 到达 Frauenkirche 或 Marienplatz
  → 与一个 NPC 对话
  → 做一个 A1 德语选择
  → 完成任务
  → 获得 10 XP 和知识卡
  → 地图状态改变
  → 刷新后仍保持完成
```

### 5.3 推荐 Game Core

新增：

```text
frontend/game-client/src/core/dialogueEngine.ts
frontend/game-client/src/core/questEngine.ts
frontend/game-client/src/core/rewardEngine.ts
frontend/game-client/src/core/conditionEvaluator.ts
```

奖励必须是纯函数：

```ts
export function applyReward(
  state: PlayerState,
  reward: Reward,
): PlayerState {
  return {
    ...state,
    moneyCents: Math.max(0, state.moneyCents + (reward.moneyCents ?? 0)),
    energy: Math.max(0, Math.min(100, state.energy + (reward.energy ?? 0))),
    germanXp: state.germanXp + (reward.germanXp ?? 0),
    discoveredPoiIds: [
      ...new Set([
        ...state.discoveredPoiIds,
        ...(reward.unlockPoiIds ?? []),
      ]),
    ],
    inventory: mergeInventory(state.inventory, reward.itemGrants ?? {}),
  }
}
```

任务完成必须幂等：

```ts
export function completeQuest(
  state: PlayerState,
  quest: Quest,
): PlayerState {
  if (state.completedQuestIds.includes(quest.id)) {
    return state
  }

  const rewarded = applyReward(state, quest.reward)
  return {
    ...rewarded,
    completedQuestIds: [...rewarded.completedQuestIds, quest.id],
  }
}
```

Pinia 只能负责协调和响应式包装：

```ts
function finishQuest(quest: Quest) {
  player.value = completeQuest(player.value, quest)
}
```

Game Core 不能 import Vue、Pinia、MapLibre、DOM 或 localStorage。

### 5.4 Exporter 修正

当前 `poi_content` 使用 JSON blob 和多版本记录。不要简单读取所有行。应先定义“最新已发布版本”。推荐数据库增加：

```sql
ALTER TABLE poi_content ADD COLUMN review_status TEXT DEFAULT 'draft';
ALTER TABLE poi_content ADD COLUMN is_published INTEGER DEFAULT 0;
```

读取时只取每类最新的已发布版本：

```sql
WITH latest AS (
  SELECT poi_id, content_type, MAX(version) AS version
  FROM poi_content
  WHERE city = ? AND is_published = 1
  GROUP BY poi_id, content_type
)
SELECT pc.*
FROM poi_content pc
JOIN latest l
  ON pc.poi_id = l.poi_id
 AND pc.content_type = l.content_type
 AND pc.version = l.version
WHERE pc.city = ?;
```

解析失败、引用不存在、任务无结束节点时应阻止发布，而不是静默返回空数组。

### 5.5 测试

必须新增：

- Dialogue 从 startNode 到 terminal；
- 不存在的 `nextNodeId` 被拒绝；
- 完成任务只奖励一次；
- 失败不会发成功奖励；
- XP、体力、金钱边界正确；
- 刷新后任务仍完成；
- 黄金 POI E2E 不调用付费 LLM。

### 5.6 验收标准

- Runtime bundle 至少有 1 NPC、1 Dialogue、1 Quest、1 KnowledgeCard；
- “开始对话”不再出现开发占位 alert；
- 任务完成后 XP 可见变化；
- 重复触发不会重复奖励；
- 刷新后任务和奖励仍存在；
- Playwright 自动完成一次黄金路径。

---

## 6. P1-01：未发现 POI 可以绕过视野限制

### 6.1 现象

灰色 POI CSS 使用：

```css
cursor: not-allowed;
```

但所有 marker 都无条件注册点击：

```ts
el.addEventListener('click', () => {
  emit('poi-click', poi)
})
```

而 `openPoi()` 又无条件发现：

```ts
markDiscovered(poiId)
currentPoiId.value = poiId
```

浏览器实测：点击灰色火车站后，已发现数量立即从 2 变为 3，并打开详情。

### 6.2 推荐规则

建议采用：

- 未发现但在视野内：点击后变为已发现并打开；
- 视野外：可以显示轮廓，但不能打开详情或开始任务；
- 已发现：始终允许查看；
- 任务进入条件另行验证距离、时间、体力和前置任务。

### 6.3 代码示例

Store 不应只接收 ID，应接收 POI 并执行规则：

```ts
type OpenPoiResult =
  | { ok: true }
  | { ok: false; reason: 'out-of-vision' }

function openPoi(poi: RuntimePoi): OpenPoiResult {
  const discovered = player.value.discoveredPoiIds.includes(poi.id)

  if (!discovered && !isInVision(poi)) {
    return { ok: false, reason: 'out-of-vision' }
  }

  if (!discovered) markDiscovered(poi.id)
  currentPoiId.value = poi.id
  isPaused.value = true
  return { ok: true }
}
```

App 显示明确反馈：

```ts
function onPoiClick(marker: PoiMarker) {
  const poi = bundle.value?.pois.find(item => item.id === marker.id)
  if (!poi) return

  const result = player.openPoi(poi)
  if (!result.ok) {
    toast.info('距离太远，先移动到附近吧')
  }
}
```

### 6.4 同时修正移动逻辑

`setPosition()` 当前只改变坐标，不触发发现检查。不要让调用者记住额外调用。可以提供更高层 action：

```ts
function moveTo(pos: PlayerPosition, pois: RuntimePoi[]) {
  setPosition(pos)
  return discoverInVision(pois)
}
```

### 6.5 验收标准

- 视野外 POI 点击不会打开；
- 视野内未发现 POI 点击后正常发现；
- 已发现 POI可以重复查看；
- 移动进入视野后 POI 状态自动更新；
- 上述行为有 store 单元测试和浏览器 E2E。

---

## 7. P1-02：移动端布局被裁切

### 7.1 实测结果

390×844 视口下：

- topbar 实际宽约 472px；
- 地图控件右边界约 460px；
- HUD 右边界约 460px；
- 页面使用 `overflow: hidden`，超出部分不可滚动，直接丢失。

### 7.2 推荐修正

不要简单缩放整个页面。应在窄屏重新布局：

```css
@media (max-width: 640px) {
  .app-shell {
    grid-template-rows: 52px 1fr;
  }

  .topbar {
    padding: 0 10px;
    min-width: 0;
  }

  .game-name {
    font-size: 16px;
    letter-spacing: 2px;
  }

  .city,
  .pmtiles-hint,
  .status .data-source {
    display: none;
  }

  .status {
    max-width: 42vw;
    overflow: hidden;
    white-space: nowrap;
    text-overflow: ellipsis;
  }

  .gaga-map-controls {
    top: 8px;
    right: 8px;
    transform: scale(0.82);
    transform-origin: top right;
  }

  .gaga-hud {
    left: 8px;
    right: 8px;
    bottom: 8px;
    width: auto;
  }

  .gaga-poi-dialog {
    left: 8px;
    right: 8px;
    bottom: 136px;
    width: auto;
    max-height: calc(100vh - 210px);
  }
}
```

具体数值可根据视觉调整，但验收必须基于实际边界，不是只看截图。

### 7.3 Playwright 测试

```ts
test.use({ viewport: { width: 390, height: 844 } })

test('mobile controls stay inside viewport', async ({ page }) => {
  await page.goto('/')

  for (const selector of ['.topbar', '.gaga-map-controls', '.gaga-hud']) {
    const box = await page.locator(selector).boundingBox()
    expect(box).not.toBeNull()
    expect(box!.x).toBeGreaterThanOrEqual(0)
    expect(box!.x + box!.width).toBeLessThanOrEqual(390)
  }
})
```

### 7.4 验收标准

- 390×844 和 768×1024 均无控件裁切；
- POI Dialog 可完整关闭；
- 音频控件不会撑破面板；
- 地图仍可拖动和缩放；
- 桌面 1280×720 视觉不回归。

---

## 8. P1-03：前后端玩家存档 schema 漂移

### 8.1 现状

前端：

```ts
PLAYER_STATE_SCHEMA_VERSION = 2
```

Python：

```py
PLAYER_STATE_SCHEMA_VERSION = 1
```

Python 缺少：

- `playerPosition`；
- `visionRadiusMeters`；
- `currentCity`。

同时 Python 文件声称与 TypeScript “严格对齐”，事实不符。

### 8.2 推荐决策

二选一：

**方案 A：暂时只有本地存档（推荐当前阶段）**

- 删除或明确标记 Python save schema 为“未启用”；
- 不宣称跨端对齐；
- 等云存档启动时再正式定义 API。

**方案 B：近期做云存档**

- Python 同步升级 v2；
- `schemaVersion` 使用 `Literal[2]`；
- 添加三个地图字段；
- 前后端共享同一份 fixture；
- 增加 v1 → v2 迁移测试。

Python v2 示例：

```py
class PlayerPosition(BaseModel):
    lng: float = Field(ge=-180, le=180)
    lat: float = Field(ge=-90, le=90)


class PlayerState(BaseModel):
    schemaVersion: Literal[2] = 2
    playerId: str
    day: int = Field(ge=0)
    minuteOfDay: int = Field(ge=0, le=1439)
    moneyCents: int = Field(ge=0)
    energy: int = Field(ge=0, le=100)
    germanXp: int = Field(ge=0)
    completedQuestIds: List[str] = Field(default_factory=list)
    discoveredPoiIds: List[str] = Field(default_factory=list)
    inventory: Dict[str, int] = Field(default_factory=dict)
    playerPosition: Optional[PlayerPosition] = None
    visionRadiusMeters: int = Field(default=500, gt=0)
    currentCity: Optional[str] = None
    lastContentVersion: Optional[str] = None
    savedAt: datetime
```

同时把 Python 的可变默认值 `[]`、`{}` 改为 `Field(default_factory=...)`。

### 8.3 验收标准

- 文档与实际部署策略一致；
- 若保留 Python schema，版本和字段与前端一致；
- v1 fixture 能迁移为 v2；
- 损坏存档有备份；
- 同一 fixture 前后端均能解析。

---

## 9. P1-04：POI DOM Marker 无法支持规模扩展

### 9.1 现状

每个 POI 创建：

- 一个 DOM 节点；
- 一个 MapLibre Marker；
- 一个独立 click listener。

3 个 POI 没问题，但德国多城市和真实 OSM POI 规模下会出现：

- DOM 数量过多；
- 监听器过多；
- 聚合困难；
- 重叠严重；
- 每次更新需要遍历 Marker。

桌面截图中 Marienplatz、Frauenkirche 和玩家坐标已经出现重叠。

### 9.2 推荐迁移

游戏 POI 使用一个 GeoJSON source：

```ts
map.addSource('game-pois', {
  type: 'geojson',
  data: poisToGeoJson(pois),
  cluster: true,
  clusterRadius: 40,
  clusterMaxZoom: 13,
})

map.addLayer({
  id: 'game-pois',
  type: 'symbol',
  source: 'game-pois',
  filter: ['!', ['has', 'point_count']],
  layout: {
    'icon-image': ['concat', 'poi-', ['get', 'type']],
    'icon-size': 0.8,
    'text-field': ['get', 'nameDe'],
    'text-offset': [0, 1.2],
    'text-size': 12,
  },
})
```

点击使用图层级事件：

```ts
map.on('click', 'game-pois', event => {
  const id = event.features?.[0]?.properties?.id
  if (typeof id === 'string') emit('poi-click', id)
})
```

玩家 marker 数量只有一个，可以继续使用 DOM Marker。

### 9.3 验收标准

- 1000 个 POI 不创建 1000 个 DOM 节点；
- 低 zoom 自动聚合；
- 中心城区 POI 不完全遮挡；
- discovered 状态通过 feature-state 或 GeoJSON property 更新；
- 点击仍能定位准确 POI。

---

## 10. P1-05：测试覆盖制造了“全绿错觉”

### 10.1 当前缺口

前端 17 个测试全部集中在内容 schema，没有测试：

- `fetchCityBundle()`；
- ETag/304 缓存；
- Player Store；
- v1 → v2 存档迁移；
- POI 发现和视野；
- MapView 生命周期；
- 移动端布局；
- 黄金任务闭环。

HTTP smoke 主要检查：

- URL 返回 200；
- 源码中是否出现某个字符串；
- HTTP header 是否存在。

它没有调用 Zod 解析 API，因此后端 200、smoke 28/28、浏览器 fallback 可以同时发生。

### 10.2 推荐测试分层

| 层 | 必测内容 |
|---|---|
| Zod/Pydantic | 字段、null、版本、引用 |
| API client | 200、304、缓存损坏、schema 失败 |
| Game Core | 任务、奖励、幂等、边界 |
| Store | 加载、保存、发现、暂停 |
| Map adapter | GeoJSON、状态映射 |
| Playwright | `[network]`、POI、任务、刷新、移动端 |

### 10.3 API client 测试示例

```ts
it('returns network data when backend payload matches schema', async () => {
  const fetcher = vi.fn().mockResolvedValue(new Response(
    JSON.stringify(runtimeApiFixture),
    {
      status: 200,
      headers: {
        ETag: 'W/"test"',
        'X-Content-Version': runtimeApiFixture.contentVersion,
      },
    },
  ))

  const result = await fetchCityBundle('munich', fetcher, localStorage)
  expect(result.source).toBe('network')
  expect(result.bundle?.city).toBe('munich')
})
```

### 10.4 验收标准

- `fetchCityBundle` 有 200、304、损坏缓存和 schema 失败测试；
- Player Store 有发现与存档迁移测试；
- 至少 1 条 Playwright 黄金路径；
- 移动端边界进入自动测试；
- smoke 失败时能指出 schema，而不是只说 HTTP 200。

---

## 11. P2-01：contentVersion 每天变化且包含未发布数据

### 11.1 问题一：同样内容第二天版本变化

当前版本包含：

```py
today_int = int(datetime.now(timezone.utc).strftime("%Y%m%d"))
return f"1.{today_int}.{digest_int:08d}"
```

即使内容完全不变，日期变化也会改变 `contentVersion`，与“相同数据产生稳定版本”的注释矛盾。

### 11.2 问题二：版本计算读取全部 scene/content

版本 hash 查询没有限定最新已发布内容。旧版本、草稿或无关记录也可能改变版本。

### 11.3 推荐修正

短期可使用纯内容 hash：

```py
digest_int = int(hashlib.sha256(canonical_bytes).hexdigest()[:8], 16)
return f"1.0.{digest_int}"
```

更推荐增加正式发布表：

```sql
CREATE TABLE runtime_releases (
  city TEXT NOT NULL,
  version TEXT NOT NULL,
  bundle_hash TEXT NOT NULL,
  published_at TEXT NOT NULL,
  PRIMARY KEY (city, version)
);
```

发布动作生成不可变 CityBundle，并分配明确 semver。API 只服务某个 release，而不是每次请求临时扫数据库重算。

### 11.4 验收标准

- 同一内容跨日期版本不变；
- 草稿变化不影响线上版本；
- 正式发布才改变版本；
- ETag 与同一 release 稳定一致；
- 可以回滚到上一个 release。

---

## 12. P2-02：FastAPI 通过静默跳过隐藏依赖故障

### 12.1 现状

`main.py` 捕获 ImportError 后不挂载 Admin routers，但 `/health` 仍返回：

```json
{"status": "ok"}
```

这会导致服务“看起来健康”，实际生成、保存或 OSM 功能缺失。

### 12.2 推荐修正

短期增加 readiness：

```py
@app.get('/api/ready')
def ready():
    capabilities = {
        'game_api': True,
        'admin_api': _HEAVY_ROUTERS_AVAILABLE,
        'db_init': _DB_SERVICE_AVAILABLE,
        'assets': ASSETS_ROOT.exists(),
    }
    if not all(capabilities.values()):
        return JSONResponse(
            status_code=503,
            content={'status': 'degraded', 'capabilities': capabilities},
        )
    return {'status': 'ready', 'capabilities': capabilities}
```

长期将 Game API 和 Admin/AI API 拆成两个 app factory 或独立服务入口，避免玩家只读 API 被 AI 依赖拖累。

### 12.3 验收标准

- `/health` 只代表进程存活；
- `/ready` 明确列出能力；
- Admin router 缺失时 readiness 为 503；
- 部署监控使用 `/ready`，不是 `/health`。

---

## 13. P2-03：资产扩展名与真实格式不一致

### 13.1 实测

审计扫描 56 个 `.png`，至少 36 个实际内容是 JPEG，包括：

- UI 图片；
- Berlin 场景；
- 多个 Munich 场景；
- Frauenkirche 季节和室内图。

FastAPI 根据扩展名返回 `image/png`，浏览器通常仍会嗅探并显示，但会造成：

- CDN/缓存 MIME 不准确；
- 图片工具链行为不一致；
- 转码和优化失效；
- 安全策略更严格时加载失败。

### 13.2 推荐修正

二选一：

- 真正转码成 PNG；或
- 将文件改名为 `.jpg` 并更新数据库/内容引用。

场景背景通常不需要透明，优先保留 JPEG/WebP；图标和 UI 若需要透明，使用真正 PNG/WebP。

增加 QA：

```py
from pathlib import Path

def detect_magic(path: Path) -> str:
    head = path.read_bytes()[:12]
    if head.startswith(b'\x89PNG\r\n\x1a\n'):
        return 'png'
    if head.startswith(b'\xff\xd8\xff'):
        return 'jpeg'
    return 'unknown'
```

发布前检查扩展名与 magic bytes 一致。

### 13.3 验收标准

- 资产扫描无扩展名/格式不一致；
- API Content-Type 与真实格式一致；
- 数据库 URL 全部更新；
- 浏览器图片回归通过。

---

## 14. P2-04：城市元数据 API 没有被客户端使用

后端 `/cities` 已返回中心、zoom、bbox 和 PMTiles URL，但前端仍硬编码：

```ts
const CENTER = [11.5755, 48.1374]
const ZOOM = 11
fetchCityBundle('munich')
setCurrentCity('munich')
```

后端登记 zoom 为 12.5，前端使用 11。多城市接口目前只是形式存在。

建议增加 `CityRegistrySchema` 和 `fetchCities()`，由当前城市元数据驱动：

```ts
const city = registry.cities.find(item => item.id === currentCityId)
if (!city) throw new Error(`Unknown city: ${currentCityId}`)

mapConfig.value = {
  center: city.center,
  zoom: city.zoom,
  pmtilesUrl: resolveAssetUrl(city.pmtilesUrl),
}
```

验收：切换城市不修改 App 源码；center/zoom/bbox/PMTiles 都来自 registry。

---

## 15. P2-05：生产包较大且尚未拆分

生产构建结果：

```text
JavaScript: 979.33 kB
gzip:      276.19 kB
```

Vite 已给出超过 500kB 警告。主要来自 MapLibre。

此项不要先于 P0/P1，但黄金闭环完成后应：

```ts
import { defineAsyncComponent } from 'vue'

const MapView = defineAsyncComponent(
  () => import('./components/MapView.vue'),
)
```

并在 Rollup 中分离 map vendor：

```ts
build: {
  rollupOptions: {
    output: {
      manualChunks: {
        map: ['maplibre-gl', 'pmtiles'],
        vue: ['vue', 'pinia'],
      },
    },
  },
}
```

验收应关注首屏可交互时间和缓存命中，不只看单个文件大小。

---

## 16. P2-06：Git 忽略规则无效、构建目录未跟踪

当前 `.gitignore`：

```gitignore
frontend/game-client/dist/        # vite build 产物(game-client)
```

Gitignore 不支持这种行尾注释，整行会被当成 pattern，因此 `dist/` 仍显示为未跟踪。

修正：

```gitignore
# Vite game-client build output
frontend/game-client/dist/
```

同时建议：

- 将机器相关 `.env.development` 改为 `.env.example`；
- 本机 URL 放 `.env.development.local`；
- 确认 `dist/` 不被提交；
- 不删除来源不明的已有未提交文件。

验收：

```bash
git check-ignore -v frontend/game-client/dist/index.html
git status --short
git diff --check
```

`git check-ignore` 必须显示命中的具体规则。

---

## 17. 建议的整改实施顺序

### Sprint A：恢复真实数据链（1–2 天）

- [ ] Python `exclude_none=True`；
- [ ] 后端实际响应通过 Zod；
- [ ] smoke 解析 schema；
- [ ] 浏览器显示 `[network]`；
- [ ] 修复 `.gitignore`；
- [ ] 增加 API client 测试。

### Sprint B：修正交互与测试（2–4 天）

- [ ] 阻止视野外 POI；
- [ ] `moveTo` 自动触发发现；
- [ ] Player Store 测试；
- [ ] 存档 schema 决策并统一；
- [ ] 390px 移动端布局；
- [ ] Playwright 桌面/移动冒烟。

### Sprint C：黄金 POI 闭环（5–8 天）

- [ ] 1 NPC；
- [ ] 1 Dialogue；
- [ ] 1 Quest；
- [ ] 1 KnowledgeCard；
- [ ] 纯 TypeScript Game Core；
- [ ] 任务幂等奖励；
- [ ] 刷新后恢复；
- [ ] Playwright 黄金路径。

### Sprint D：扩展准备（3–5 天）

- [ ] POI 改 GeoJSON layer；
- [ ] 城市 registry 驱动客户端；
- [ ] content release 机制；
- [ ] 素材格式 QA；
- [ ] bundle code splitting。

---

## 18. 整改后的统一验收命令

```bash
cd /Volumes/NewDisk/GermanLearning/frontend/game-client
npm run typecheck
npm test
npm run build
npm run test:e2e

cd /Volumes/NewDisk/GermanLearning
PYTHONDONTWRITEBYTECODE=1 ./.venv/bin/python -m pytest -p no:cacheprovider \
  backend/poi-generator/schemas/tests \
  backend/poi-generator/tests -q

git diff --check
git status --short
```

浏览器人工验收：

1. 顶部显示 `[network]`；
2. 控制台无 Zod warning；
3. 地图、POI、场景图、音频正常；
4. 视野外 POI不能打开；
5. 完成一个任务后 XP 增加；
6. 刷新后任务仍完成；
7. 390×844 下 UI 不裁切。

---

## 19. Agent 交接要求

每个整改 Agent 完成后必须报告：

```md
## 已修复
- 对应审计编号：P0-01 / P1-01 ...

## 修改文件
- path：修改原因

## 测试
- 命令：结果
- 浏览器步骤：结果

## 数据和存档影响
- 是否需要 DB migration
- 是否需要清缓存
- 是否影响旧存档

## 尚未解决
- 明确列出，不得用“基本完成”掩盖
```

禁止仅根据 typecheck 或 HTTP 200 宣称完成。必须验证用户实际链路。

---

## 20. 项目下一步方向（简要）

1. 先修复 Runtime API 与 Zod 契约，确保使用真实网络数据；
2. 完成一个黄金 POI 的德语任务闭环；
3. 补齐浏览器 E2E、移动端和存档测试；
4. 将 POI 迁移到 GeoJSON 图层并建立正式内容发布版本；
5. 闭环验证成功后再增加城市、Phaser 场景和桌面包装。
