# ── gagaToday Game Client · 生产构建配置 ──
# 真实密钥仍在 /Volumes/NewDisk/.agent-secrets/secrets.env,不入仓

# PMTiles — 用相对路径,通过 web 容器的 nginx 反代到后端
VITE_PMTILES_URL=/assets/munich_map/pmtiles/germany-zoom16.pmtiles

# Game API — 相对路径
VITE_GAME_API_URL=/api

# Bundle 版本(留空永远拿最新)
VITE_BUNDLE_CONTENT_VERSION=
