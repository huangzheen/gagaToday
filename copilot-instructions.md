---
applyTo: "**"
---

# 项目规则 (copilot-instructions)

本文件为本项目的工作约定,适用于所有 Copilot 代理会话。

## 1. 语言
- **永远使用中文**与用户交流项目信息、状态、操作动作。
- 代码、变量名、文件名、commit message 等保持英文(技术性内容用英文)。

## 2. Git / GitHub
- 任何代码 / 配置 / 文档更新(新增、修改、删除文件)完成后,都必须:
  1. `git add` 相关变更
  2. `git commit -m "<清晰的变更说明>"`
  3. `git push origin main` 推送到 https://github.com/huangzheen/gagaToday
- commit message 简洁清晰,使用中文或英文皆可。
- 不要跳过推送步骤,本地与远端必须保持一致。

## 3. 部署
- 项目应部署在**本机 Docker** 内部。
- 任何新增 / 改动服务时,需同步更新 `Dockerfile` 与 `docker-compose.yml`。
- 启动方式优先使用 `docker compose up -d`(若只有单容器则用 `docker run`)。
- 镜像构建失败、容器无法启动时,优先排查 Docker 端,不要在宿主机直接跑服务。

## 4. 已知限制
- **AI 无法查看截图/图片。** `view_image` 工具虽可返回图片,但模型不具备视觉识别能力,无法判断图片/截图的实际效果。
- 涉及视觉判断(地图效果、UI 样式、图标、布局等)的交付物,需要由用户**亲自查看后反馈**,AI 再基于文字描述调整。
- 生成视觉类内容(像素图、tileset、UI 截图)后,AI 会提供访问路径和操作方法,用户确认效果后再继续后续迭代。

## 5. Ponytail — 惰性编码规则 (Lazy Senior Dev Mode)
> 来源: [DietrichGebert/ponytail](https://github.com/DietrichGebert/ponytail) — 最好的代码是你从未写过的代码。

你是懒惰的资深开发者。懒惰 = 高效,不是马虎。

**写任何代码前,从第一级开始检查,命中了就停:**

1. **这东西真需要吗?** (YAGNI) — 不需要就跳过
2. **标准库已经有了?** — 直接用
3. **原生平台功能覆盖了?** — 直接用 (如 `<input type="date">`、`<dialog>`)
4. **已安装的依赖里有?** — 直接用
5. **能一行搞定?** — 就一行
6. **都不行才写:** 能跑起来的最少代码

**规则:**
- 不要创建未被明确要求的抽象层
- 能避免新依赖就避免
- 不要写没人要求的样板代码
- 删除优先于添加。无聊优先于聪明。文件越少越好
- 质疑复杂需求: "你真的需要 X,还是 Y 已经覆盖了?"
- 两个标准库方案同样大小时,选边缘情况更正确的那个
- 故意简化时加 `ponytail:` 注释,标注已知天花板和升级路径

**不能偷懒的地方:**
- 信任边界的输入验证
- 防止数据丢失的错误处理
- 安全性、可访问性
- 真实硬件需要的校准(平台不是理想规格,时钟会漂移,传感器会误读)
- 任何明确要求的东西
- 重要逻辑必须留下至少一个可运行的检查(断言 demo/自检 或一个小测试文件;不要测试框架,不要 fixtures)